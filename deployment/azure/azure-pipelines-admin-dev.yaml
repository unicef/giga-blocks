trigger:
  branches:
    include:
    - dev
  paths:
    include:
    - apps/admin/**

pool:
  vmImage: ubuntu-latest

variables:
  containerRegistryServiceConnection: $(ACR_SERVICE_CONNECTION)
  imageTag: '$(Build.BuildId)'
  system.debug: true
  CI: false
  env_dev: $(ENV_DEV)

stages:
- stage: Build
  displayName: 'GIGA NFT 2.0 admin build'
  jobs:
  - job: Build
    displayName: 'GIGA NFT 2.0 admin build job'

    steps:
    - checkout: self
      fetchTags: true

    - script: |
        touch ./.env && echo "$(env_dev)" >> ./.env
      displayName: 'import env file'

    - task: NodeTool@0
      inputs:
        versionSpec: '18.x'
      displayName: 'Use Node.js'

    - script: |
        npm install -g pnpm
        pnpm install
        npx nx build admin
      displayName: 'install dependencies and build apps'

    - task: Docker@2
      displayName: Login to container Registry
      inputs:
        command: login
        containerRegistry: $(containerRegistryServiceConnection)

    - task: Docker@2
      displayName: Build and push image to container registry
      inputs:
        command: buildAndPush
        repository: 'giga-ntf2-admin'
        dockerfile: './Dockerfile.admin'
        containerRegistry: $(containerRegistryServiceConnection)
        tags: |
          $(imageTag)
